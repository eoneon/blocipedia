<!DOCTYPE html>
<html>
<head>
  <title>Blocipedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <div class="container">
      <ul class="nav nav-tabs">
        <li><%= link_to "Blocipedia", root_path %></li>
        <li><%= link_to "Wikis", wikis_path %></li>
      </ul>
      <div class="pull-right user-info">
        <% if user_signed_in? %>
          Signed in as: <strong><%= link_to current_user.username, user_path(current_user) %></strong> <br/>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
        <% end %>

        <% if ChargePolicy.new(current_user, nil).charge? %>
          | <%= link_to "Updgrade to Premium", new_charge_path %>
        <% elsif ChargePolicy.new(current_user, nil).refund? %>
          | <%= link_to "Downgrade to Standard", refund_path, method: :post %>
        <% end %>

        <% if not user_signed_in? %>
          <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link'  %> -
          <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link'  %>
        <% end %>

      </div>
      <div class="flash">
        <% if flash[:notice] %>
          <div class="alert alert-dismissible alert-success">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            <%= flash[:notice] %>
          </div>
        <% end %>
        <% if flash[:alert] %>
          <div class="alert alert-dismissible alert-warning">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            <%= flash[:alert] %>
          </div>
        <% end %>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
